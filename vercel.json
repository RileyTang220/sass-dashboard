{
  "buildCommand": "DATABASE_URL=\"$POSTGRES_PRISMA_URL\" npx prisma generate --schema=server/prisma/schema.prisma && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/((?!api/|assets/|.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
